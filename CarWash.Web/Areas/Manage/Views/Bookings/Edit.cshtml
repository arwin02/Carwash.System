@model CarWash.Web.Areas.Manage.ViewModels.bookings.EditViewModel
@{
    Layout = "~/areas/manage/views/shared/_forms.cshtml";
}

<br /><br /><br />
<br />
<h1 class="text-center">Edit Bookings</h1>
<hr />
<br /><br />
<div class="container">
    <div class="row">
        <div class="col">
            <form method="post" action="~/manage/booking/bookings-edit">
                @Html.AntiForgeryToken()
                @Html.ValidationSummary()
                <input type="hidden" name="Id" value="@Model.Id" />

                <div class="form-group">
                    <label for="serviceType" class="control-label mb-1">ServiceType</label>
                    <select class="form-control" id="serviceType" name="ServiceType">
                        @{
                            var options = Enum.GetValues(typeof(CarWash.Web.Infrastructures.Domain.Enums.ServiceType)).Cast<CarWash.Web.Infrastructures.Domain.Enums.ServiceType>();

                            foreach (CarWash.Web.Infrastructures.Domain.Enums.ServiceType option in options)
                            {
                                <option value="@option">@option.ToString()</option>
                            }
                        }
                    </select>
                </div>
                <div class="form-group">
                    <label for="time" class="control-label mb-2">Time</label>
                    <input type="text" class="form-control" id="time" name="Time" aria-describedby="timeHelp" value="@Model.Time" placeholder="Enter Time">
                </div>

                <div class="form-group">
                    <label for="bookingaddress" class="control-label mb-2">Booking Address</label>
                    <input type="text" class="form-control" id="bookingaddress" name="BookingAddress" aria-describedby="bookingaddressHelp" value="@Model.BookingAddress" placeholder="Enter Description">
                </div>

                <div class="form-group">
                    <label for="vehicle" class="control-label mb-2">Vehicle</label>
                    <input type="text" class="form-control" id="vehicle" name="Vehicle" value="@Model.Vehicle" aria-describedby="vehicleHelp" placeholder="Enter vehicle">
                </div>
                @*<div class="form-group">
                    <label for="price" class="control-label mb-2">Price</label>
                    <input type="text" class="form-control" id="price" name="Price" value="@Model.Price" aria-describedby="priceHelp" placeholder="Enter Price">
                </div>*@

                <div class="mt-2">
                    <button type="submit" class="btn btn-primary">Submit</button>
                    <a href="~/manage/booking/details/@Model.Id" class="btn btn-danger">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>
